//Window object methods

function confirmBoxFunc(){
    var confirmRes = confirm("Are you sure you want to delete the record?")
    if(confirmRes == true)
        alert("Deleted the record successfully...")    
    else
        alert("cancel the operation...")
}

//Window object methods

function confirmBoxFunc(){
    var confirmRes = confirm("Are you sure you want to delete the record?")
    if(confirmRes == true)
        alert("Deleted the record successfully...")    
    else
        alert("cancel the operation...")
}
